# Glora Step 4 default config (foundation + LoRA few-shot).

base: artifacts/step3/foundation_base.pt
target: mobilenet_v2
batch_size: 1
episodes: 80
batch_episodes: 4
mini_batch_size: 128
ppo_epochs: 3
lr: 5.0e-4

rank: 8
alpha: 16
lora_dropout: 0.0
target_modules: linear1,linear2

streams: 8
iters: 15
warmups: 5
seed: 0
